import Vue from 'vue';
import Vuetify from 'vuetify/lib/framework';

Vue.use(Vuetify);

export default new Vuetify({
    theme: { dark: true }, //Linha que foi copiada para fazer o tema ficar escuro/dark
});
